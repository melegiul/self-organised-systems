\documentclass[
fontsize=12pt,          % Schriftgröße
paper=a4,               % Papiergröße
captions=tableabove,    % Beschriftungen für Tabellen oberhalb
titlepage=firstiscover, % Titelseite ist Umschlagseite
BCOR=5mm,               % Korrektur für Bundsteg
toc=listof,             % Abbildungs- und Tabellenverzeichnis im Inhaltsverzeichnis
open=right,             % Kapitel beginnen auf rechter Seite
]{scrreprt}

% ----------------------------------------------------
% Essential packages
% ----------------------------------------------------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{verbatim}

% ----------------------------------------------------
% Packages for layout adjustments
% ----------------------------------------------------

% Adjust line spacing
\usepackage{setspace}

% Publication quality tables
\usepackage{booktabs}
\usepackage[utf8]{inputenc}

% ----------------------------------------------------
% Fonts
% ----------------------------------------------------
\usepackage{lmodern}
\usepackage{roboto}

% further packages
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{mathtools}
\DeclarePairedDelimiter{\card}{\lvert}{\rvert}
\usepackage{subfigure}

% diagram packages
\usepackage{siunitx}
\usepackage{tikz}
\usepackage{pgfplots}

\pgfplotsset{compat=newest} % Allows to place the legend below plot
\usepgfplotslibrary{units} % Allows to enter the units nicely

\begin{document}
	\chapter{Aufgabe 4}
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={simple,steep,random},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/simHC/bb0-1.csv}; 
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/steepHC/bb0-1.csv};
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/randomHC/bb0-1-50.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={simple,steep,random},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/simHC/bb1-1.csv}; 
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/steepHC/bb1-1.csv};
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/randomHC/bb1-1-50.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	\newline
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={simple,steep,random},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/simHC/bb2-1.csv}; 
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/steepHC/bb2-1.csv};
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/randomHC/bb2-1-50.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={simple,steep,random},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/simHC/bb3-1.csv}; 
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/steepHC/bb3-1.csv};
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/randomHC/bb3-1-50.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	\newline
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={simple,steep,random},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/simHC/bb4-1.csv}; 
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/steepHC/bb4-1.csv};
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/randomHC/bb4-1-50.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	%\end{comment}
	\newpage
	
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={simple,steep,random},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/simHC/bb0-10.csv}; 
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/steepHC/bb0-10.csv};
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/randomHC/bb0-10-50.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={simple,steep,random},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/simHC/bb1-10.csv}; 
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/steepHC/bb1-10.csv};
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/randomHC/bb1-10-50.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	\newline
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={simple,steep,random},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/simHC/bb2-10.csv}; 
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/steepHC/bb2-10.csv};
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/randomHC/bb2-10-50.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={simple,steep,random},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/simHC/bb3-10.csv}; 
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/steepHC/bb3-10.csv};
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/randomHC/bb3-10-50.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	\newline
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={simple,steep,random},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/simHC/bb4-10.csv}; 
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/steepHC/bb4-10.csv};
	\addplot 
	% add a plot from table; you select the columns by using the actual name in
	% the .csv file (on top)
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/randomHC/bb4-10-50.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}	
	\newpage	
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={linear 0.5, linear 0.8, expon 0.5, expon 0.8},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/linear/bb0-0.5-0.5.csv}; 
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/linear/bb0-0.8-0.8.csv};
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/exponentially/bb0-0.5-0.5.csv};
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/exponentially/bb0-0.8-0.8.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={linear 0.5, linear 0.8, expon 0.5, expon 0.8},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/linear/bb1-0.5-0.5.csv}; 
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/linear/bb1-0.8-0.8.csv};
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/exponentially/bb1-0.5-0.5.csv};
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/exponentially/bb1-0.8-0.8.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	\newline
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={linear 0.5, linear 0.8, expon 0.5, expon 0.8},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/linear/bb2-0.5-0.5.csv}; 
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/linear/bb2-0.8-0.8.csv};
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/exponentially/bb2-0.5-0.5.csv};
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/exponentially/bb2-0.8-0.8.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={linear 0.5, linear 0.8, expon 0.5, expon 0.8},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/linear/bb3-0.5-0.5.csv}; 
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/linear/bb3-0.8-0.8.csv};
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/exponentially/bb3-0.5-0.5.csv};
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/exponentially/bb3-0.8-0.8.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	\newline
	\begin{tikzpicture}
	\begin{axis}[
	width=7cm, % Scale the plot to \linewidth
	grid=major, % Display a grid
	legend entries={linear 0.5, linear 0.8, expon 0.5, expon 0.8},
	grid style={dashed,gray!30}, % Set the style
	%xlabel=Iteration, % Set the labels
	%ylabel=Optimumfitness,
	%legend style={at={(0.5,-0.2)},anchor=north}, % Put the legend below the plot
	x tick label style={rotate=90,anchor=east} % Display labels sideways
	]
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/linear/bb4-0.5-0.5.csv}; 
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/linear/bb4-0.8-0.8.csv};
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/exponentially/bb4-0.5-0.5.csv};
	\addplot 
	table[x=iteration,y=value,col sep=comma] {/home/giuliano/oc2-mango/blackboxPython/exponentially/bb4-0.8-0.8.csv};
	%\legend{Plot}
	\end{axis}
	\end{tikzpicture}
	
\end{document}